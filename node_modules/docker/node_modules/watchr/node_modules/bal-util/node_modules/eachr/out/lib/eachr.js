// Generated by CoffeeScript 1.9.1
(function() {
  var eachr, typeChecker,
    hasProp = {}.hasOwnProperty;

  typeChecker = require('typechecker');

  eachr = function(subject, callback, context) {
    var i, key, len, value;
    if (typeChecker.isArray(subject)) {
      for (key = i = 0, len = subject.length; i < len; key = ++i) {
        value = subject[key];
        if (callback.apply(subject, [value, key, subject]) === false) {
          break;
        }
      }
    } else {
      for (key in subject) {
        if (!hasProp.call(subject, key)) continue;
        value = subject[key];
        if (callback.apply(subject, [value, key, subject]) === false) {
          break;
        }
      }
    }
    return subject;
  };

  module.exports = eachr;

}).call(this);
